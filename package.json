{
  "name": "andrewos",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "postinstall": "playwright install chromium",
    "test": "vitest run",
    "test:ui": "playwright test",
    "test:all": "npm test && npm run test:ui"
  },
  "dependencies": {
    "jquery.terminal": "^2.39.3",
    "openai": "^4.29.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "pngjs": "^7.0.0",
    "serve": "^14.2.4",
    "vitest": "^3.2.4"
  }
}
